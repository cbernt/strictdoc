from typing import Optional

# from tomlkit import parse


class SDocConfig:
    def __init__(self):
        pass


class ConfigParser:
    @staticmethod
    def parse_config() -> Optional[SDocConfig]:
        # with open("strictdoc.toml", "r") as config_file:
        #     config_content = config_file.read()
        # config_dict = parse(config_content)
        return None
